
const Onebook = ({Onebookdetails}) => {

    const {bookName} = Onebookdetails
    return (
        <div>

            <h1>{bookName}</h1>
            
        </div>
    );
};

export default Onebook;